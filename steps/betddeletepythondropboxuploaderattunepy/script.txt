$DIRECTORY = "C:\Users\Public\Downloads"
cd $DIRECTORY

$FILE = "PythonDropboxUploader-Attune.py"

if (Test-Path -Path $FILE -PathType Leaf) {
    Remove-Item "$FILE"
    echo "Deleted $DIRECTORY\$FILE"
}
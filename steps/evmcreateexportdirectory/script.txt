$FOLDER = "{hypervExportDirectory}"

if (Test-Path $FOLDER) {
  Write-Host "Export folder $FOLDER exists, good"
} else {
  Write-Host "Export folder $FOLDER deos not exist, creating"
  New-Item $FOLDER -ItemType Directory
  if (Test-Path $FOLDER) {
    Write-Host "Export folder $FOLDER created."
  }
}
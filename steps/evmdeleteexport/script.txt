$VM = '{targetServer.name}'

cd {hypervExportDirectory}

$EXPORT_FILE = "$VM"

if (Test-Path $EXPORT_FILE) {
  Write-Host "Deleting $EXPORT_FILE."
  Remove-Item -Recurse $EXPORT_FILE
} else {
  Write-Host "$EXPORT_FILE doesn't exist, skipping."
}